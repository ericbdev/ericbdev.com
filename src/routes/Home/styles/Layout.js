import styled from 'styled-components';
import mixins from '../../../styles/mixins';

export const Layout = styled.div`
  ${mixins.layoutOuter};
  align-items: center;
  position: relative;
  min-height: 100vh;
`;
